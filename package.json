{
  "name": "railway-management-mern",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && NODE_OPTIONS=--openssl-legacy-provider npm run build",
    "start": "cd server && npm install && node server.js"
  }
}